apiVersion: apps/v1
kind: Deployment
metadata:
  name: question-service
  namespace: interview-coach
  labels:
    app: question-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: question-service
  template:
    metadata:
      labels:
        app: question-service
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8082"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: question-service
          image: ghcr.io/interview-coach/question-service:latest
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: interview-coach-config
          env:
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: interview-coach-secret
                  key: DATABASE_PASSWORD
            - name: CLAUDE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: interview-coach-secret
                  key: CLAUDE_API_KEY
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: interview-coach-secret
                  key: OPENAI_API_KEY
          resources:
            requests:
              memory: "640Mi"
              cpu: "500m"
            limits:
              memory: "768Mi"
              cpu: "1000m"
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 20
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 15
